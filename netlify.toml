[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Official Next.js runtime — handles App Router SSR, the /api/chat route,
# and streaming. Netlify auto-installs this plugin during the build.
[[plugins]]
  package = "@netlify/plugin-nextjs"
